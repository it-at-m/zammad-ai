[project]
name = "slm-guardrails"
license = "MIT"
version = "1.1.0"

description = "Content safety guardrail service for Zammad AI (FastAPI)"
readme = "README.md"
requires-python = "==3.14.5"
authors = [{ name = "Zammad-AI Team" }]

dependencies = [
    "fastapi==0.141.1",
    "httpx==0.28.1",
    "pydantic==2.13.4",
    "pydantic-settings==2.15.0",
    "python-dotenv==1.2.3",
    "structlog==26.1.0",
    "truststore==0.10.4",
    "uvicorn==0.52.4",
    "prometheus_client==0.26.0",
    "gliner2==2.0.0",
    "gliclass==0.1.20",
    "torch==2.13.0",
    "transformers==5.16.1",
    "setuptools==84.0.0",
]

[dependency-groups]
dev = ["pytest==9.1.1", "pytest-asyncio==1.4.0", "ruff==0.16.4", "ty==0.0.75"]

[tool.uv]
add-bounds = "exact"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["test"]
asyncio_mode = "auto"
